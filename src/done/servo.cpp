// #include <Arduino.h>
// #include <Wire.h>
// #include <Servo.h>

// Servo myservo;
// const int servoPin = 12; // Or your servo pin

// void setup() {
//   myservo.attach(servoPin);
// }

// void loop() {
//   // Rotate 90 degrees clockwise (adjust speed and duration)
//   myservo.write(45);  // Adjust this value (closer to 0) for faster CW rotation
//   delay(420);       // Adjust this duration (milliseconds) for 90-degree rotation

//   myservo.write(90); // Stop
//   delay(2000);      // Wait 2 seconds

//   // Rotate 90 degrees counter-clockwise (adjust speed and duration)
//   myservo.write(135); // Adjust this value (closer to 180) for faster CCW rotation
//   delay(410);       // Adjust this duration (milliseconds) for 90-degree rotation

//   myservo.write(90); // Stop
//   delay(2000);      // Wait 2 seconds
// }